# GEMINI Env Settings

1. always 
2. use context7
3. 

# Dev Env Settings.

1. PowerShell에서 파일 저장 시 기본 인코딩(UTF-16LE)로 저장하면 한글이 깨질 수 있다. 반드시 UTF-8을 명시해서 읽고/쓴다.
2. 모든 대화는 아래와 같은 양식으로 `dialog.md`파일에 기록한다. 질문 - 답변 한 세트로 기록한다.
   `yy.mm.dd hh:mm`
   ***
   **질문**
   **답변**
   `줄 바꿈`

3. 